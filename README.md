# Codegen Client Figma API

This script will scrape the Figma API documentation and generate the TypeScript client.

### Generate the client

```bash
nvm use
pnpm install
pnpm build && pnpm generate
```
